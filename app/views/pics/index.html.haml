- if @pics.count > 0 
  - @pics.each do |pic|
    = link_to (image_tag pic.image.url(:medium)), pic
    %h2= link_to pic.title, pic
- else 
  No pictures yet. You can upload them 
  = link_to("here", new_pic_path)
  